# Local dev: leave unset. Vite's dev-server proxy (vite.config.ts) forwards
# /api and /ws to http://localhost:8080 as same-origin requests.

# Set only when the dashboard is deployed separately from the API (e.g.
# Render static site talking to a Render web service on another domain).
# No trailing slash.
VITE_API_URL=
